#ifndef GLOBALS_H
#define GLOBALS_H

extern int rows;         // The rows of the graph.
extern int *row_sums;    // The number of elements on every row.
extern int *indexes;     // An array with the new indexes after graph coloring.
extern int num_thread;

#endif
